# Factory-Link — environment templates
#
# Docker Compose (single source of truth for backend):
#   cp devops/.env.example devops/.env
#
# Vite client (local npm run dev):
#   cp client/.env.example client/.env
#
# Optional service-specific examples:
#   server-node/.env.example
#   server-ai/.env.example
