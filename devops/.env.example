# Copy to devops/.env and adjust values.
# docker compose --env-file devops/.env -f devops/docker-compose.yml up --build

DB_ROOT_PASSWORD=rootpassword
DB_USER=factorylink
DB_PASS=factorylink123
DB_NAME=factory_link

JWT_SECRET=your_jwt_secret_here_min_32_chars_long_value

OPENAI_API_KEY=

# Optional local overrides for Spring (Docker sets DB_* above)
# SPRING_DATASOURCE_URL=jdbc:mariadb://mariadb:3306/factory_link
