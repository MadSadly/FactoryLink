<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.factorylink.mapper.QuoteMapper">

  <select id="selectById" resultType="com.factorylink.entity.Quote">
    SELECT id, requester_company_id AS requesterCompanyId, target_company_id AS targetCompanyId,
           items_json AS itemsJson, raw_input AS rawInput, parsed_requirements AS parsedRequirements,
           quote_html AS quoteHtml, quote_text AS quoteText, total_amount AS totalAmount,
           deadline, valid_until AS validUntil, notes, status, pdf_path AS pdfPath,
           created_at AS createdAt, updated_at AS updatedAt
    FROM quotes WHERE id = #{id}
  </select>

  <update id="updatePdfPath">
    UPDATE quotes SET pdf_path = #{pdfPath}, updated_at = NOW() WHERE id = #{id}
  </update>
</mapper>
