<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.factorylink.mapper.RecommendMapper">

  <insert id="insertFeedback">
    INSERT INTO recommendation_feedback (query_company_id, recommended_company_id, score, action)
    VALUES (#{queryCompanyId}, #{recommendedCompanyId}, #{score}, #{action})
  </insert>
</mapper>
