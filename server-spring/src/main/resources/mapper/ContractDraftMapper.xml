<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.factorylink.mapper.ContractDraftMapper">

  <select id="selectById" resultType="com.factorylink.entity.ContractDraft">
    SELECT id, quote_id AS quoteId, contract_html AS contractHtml, contract_text AS contractText,
           payment_terms AS paymentTerms, warranty_months AS warrantyMonths, special_terms AS specialTerms,
           pdf_path AS pdfPath, status, created_at AS createdAt
    FROM contract_drafts WHERE id = #{id}
  </select>

  <update id="updatePdfPath">
    UPDATE contract_drafts SET pdf_path = #{pdfPath} WHERE id = #{id}
  </update>
</mapper>
